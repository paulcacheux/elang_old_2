//Allow for variable assignment and use
//10
//10
//100
//120
//10000

{
  x=10;
  y = x;
  z= x * y + 10 - 2*5;
  sum =x + y + z;

  println(x);
  println(y);
  println(z);
  println(sum);
  println(x*y*z);
}
