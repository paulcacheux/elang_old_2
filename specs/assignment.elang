//Allow for variable assignment and use
//10
//10
//100
//120
//10000

fn main()
{
  let x=10;
  let y = x;
  let z= x * y + 10 - 2*5;
  let sum =x + y + z;

  println(x);
  println(y);
  println(z);
  println(sum);
  println(x*y*z);
}
